function output = layer_remove(initial_layer) 
% This function checks whether there is a zero present in the vector that
% describes the network structure. If there is a zero it deletes the layers
% that comes after the zero thus restricting the size of the network. 








end